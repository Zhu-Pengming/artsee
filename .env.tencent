# 腾讯云部署配置
# 使用方法：source .env.tencent && npm run deploy:tencent

# 腾讯云服务器地址（artiqore.com）
export TENCENT_HOST=artiqore.com

# SSH 配置
export TENCENT_USER=ubuntu
export TENCENT_KEY=~/tom.pem

# 远程部署目录
export TENCENT_DIR=~/website/artsee

# 可选配置
# export SKIP_BUILD=1
# export PRESERVE_REMOTE_ENV=1
